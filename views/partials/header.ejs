<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body>
  <header class="topbar">
    <div>
      <a class="brand" href="/dashboard">Spreetail</a>
      <p class="subtitle">Shared expenses with traceable import decisions.</p>
    </div>
    <% if (user) { %>
      <form method="post" action="/logout">
        <button class="ghost" type="submit">Logout</button>
      </form>
    <% } %>
  </header>
  <main class="shell">
